# `record_agent_experiment_event`

Append a trajectory event such as observation, action, tool_call, reward, note, or error.

| Field | Value |
| --- | --- |
| MCP page name | `record_agent_experiment_event` |
| Current implementation | `record_agent_experiment_event` |
| Auth | Google/Dex OIDC bearer token, or temporary service bearer token |

Use this tool from a connected coding agent through `https://security-rl.useimmaculate.com/mcp`.
