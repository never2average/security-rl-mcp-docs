# `get_blue_team_fix_queue`

Return active Red findings this Blue agent must fix, with due versions and sanitized state.

| Field | Value |
| --- | --- |
| MCP page name | `get_blue_team_fix_queue` |
| Current implementation | `get_blue_team_fix_queue` |
| Auth | Google/Dex OIDC bearer token, or temporary service bearer token |

Use this tool from a connected coding agent through `https://security-rl.useimmaculate.com/mcp`.
