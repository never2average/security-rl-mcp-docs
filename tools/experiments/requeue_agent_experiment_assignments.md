# `requeue_agent_experiment_assignments`

Move failed Codex/Claude Code assignments back to queued after provider recovery.

| Field | Value |
| --- | --- |
| MCP page name | `requeue_agent_experiment_assignments` |
| Current implementation | `requeue_agent_experiment_assignments` |
| Auth | Google/Dex OIDC bearer token, or temporary service bearer token |

Use this tool from a connected coding agent through `https://security-rl.useimmaculate.com/mcp`.
