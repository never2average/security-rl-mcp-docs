# Assignments

Assignments are leased units of work for external agents. They allow many Codex, Claude Code, or Eve workers to run concurrently without two workers taking the same job.

```text
power user -> local coding agent -> RL Environment MCP -> Postgres + runtime workspaces + reward data
```
