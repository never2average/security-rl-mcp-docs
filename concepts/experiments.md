# Experiments

Experiments group assignments, target mode, provider mix, teams, and evaluation outputs. Type 1 improves the customer state. Type 2 evaluates and trains the Blue-team policy.

```text
power user -> local coding agent -> RL Environment MCP -> Postgres + runtime workspaces + reward data
```
