# state.json

`state.json` is the application-state artifact agents submit and improve. It can be sent in one request or in named top-level sections, then finalized for schema validation.

```text
power user -> local coding agent -> RL Environment MCP -> Postgres + runtime workspaces + reward data
```
