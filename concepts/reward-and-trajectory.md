# Reward and Trajectory

Trajectory events record observations, actions, tool calls, fixes, issues, notes, errors, and reward deltas. Evaluation tools turn those events into comparable RL rewards.

```text
power user -> local coding agent -> RL Environment MCP -> Postgres + runtime workspaces + reward data
```
